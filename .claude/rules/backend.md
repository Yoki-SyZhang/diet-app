---
paths:
  - "backend/**/*.py"
---

# Backend rules

- 修改前先读取 `docs/product/SPEC.md` 中对应章节；食物业务以 §7 为唯一流程定义。
- API 层只负责边界转换，业务规则进入共享服务，避免对话入口与手动入口各写一套逻辑。
- 未经用户明确确认，不运行真实数据库迁移，不读取或修改真实 SQLite 数据文件。
- 完成后运行相关后端单元与集成测试；命令尚未定义时必须明确报告未验证项。
